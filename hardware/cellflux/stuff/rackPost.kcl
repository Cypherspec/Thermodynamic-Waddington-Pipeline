// Rack support foot
// Short drilled standoff below the reagent rack.
@settings(defaultLengthUnit = mm, kclVersion = 2.0)

import tube from "geometry.kcl"
post = tube(outerDiameter = 10mm, innerDiameter = 3mm, height = 30mm)
  |> appearance(color = "#91a7b4", metalness = 75)
